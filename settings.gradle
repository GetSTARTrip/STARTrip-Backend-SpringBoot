pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/release' }
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = 'startrip-codebase'
include 'startrip-api'
include 'startrip-core'
include 'startrip-batch'

